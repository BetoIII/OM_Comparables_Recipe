# Example environment variables for OM Comparables Recipe
# Copy this file to comparables-app/.env.local and customize as needed

# Optional: Custom paths for data files (defaults shown)
# DATA_PATH=../output/comparables_data.json
# COMP_SETS_DIR=../comp_sets

# Optional: Custom port for Next.js development server
# PORT=3001

# Note: The application works without these environment variables
# using the default relative paths. Only set these if you need
# to customize the file locations.
